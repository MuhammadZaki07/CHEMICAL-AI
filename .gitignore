# Laravel environment & sensitive files
.env
.env.backup
.env.production
/storage/*.key

# Composer / PHP dependencies
/vendor
/composer.lock
/composer.phar

# Node / build (tidak perlu push build ke production jika sudah di-deploy)
node_modules
/public/build
/public/hot
npm-debug.log
yarn-error.log
package-lock.json
yarn.lock

# IDE & local config
/.idea
/.vscode
/.fleet
/.phpunit.cache
/.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json

# OS generated files
.DS_Store
Thumbs.db

# Jangan ignore index.php dan .htaccess karena mereka ada di root production
/index.php
/.htaccess
